doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    link(href="https://use.fontawesome.com/releases/v5.1.0/css/all.css" rel="stylesheet")
    link(rel="stylesheet" type='text/css' href="/style.css")
    script(src='../js/logout.js')
    script(src='../js/sidebar.js')
    title= title
  body.gradient2
    div.wrapper
      include sideBar.pug
      div.center
        span.search
          img(src='/images/Hamburger_icon.svg.png').burger
          form(method='post' action='/search')
            label(for='search')
            input(type='text' class='input-search' placeholder='search coredump' name='search' required)
            input(type='submit' name='searchBtn').btn-search
          if !signedIn
            a(href='/login').login Login
            a(href='/signup').signup Sign Up
          else
            div.avatar-container
              span.logout Log Out
              span.avatar
              span.navbar-text Welcome #{signedIn.dataValues.userName}!
              a(href='/')
        div.main-box
        block content
        div.footer
          p © Copyright: App Academy Group Project - July Cohort 2020
